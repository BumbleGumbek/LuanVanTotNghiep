
<section class="shopping-cart spad">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="breadcrumb__links">
                    <a href="/">Home</a>
                    <a href="/my-orders">My Orders</a>
                    <span>Order Details</span>
                </div>
            </div>
            <div class="col-lg-12">
                <div class="section-title" style="text-align: left; margin-bottom: 30px;">
                    <h2>Order</h2>
                    <h3>{{shortId order._id}}</h3>
                    <p>{{formatDate order.createdAt}}</p>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-lg-8">
                <div class="shopping__cart__table">
                    <table>
                        <thead>
                        <tr>
                            <th>Jewelry</th>
                            <th>Size</th>
                            <th style="width:160px;text-align:center;">Price</th>
                            <th style="width:100px;text-align:center;">
                                Quantity
                            </th>

                            <th style="width:160px;text-align:right;">
                                Total
                            </th>
                        </tr>
                        </thead>
                        <tbody>
                        {{#each order.items}}
                            <tr>
                                <td class="product__cart__item" style="width: 350px;">
                                    <div class="product__cart__item__pic">
                                        <img src="{{this.image}}" alt="{{this.name}}" width="90">
                                    </div>
                                    <div class="product__cart__item__text">
                                        <h6>{{this.name}}</h6>
                                    </div>
                                </td>
                                <td class="cart__price" style="width: 80px; text-align: center;">
                                    <span class="badge badge-light" style="font-size: 14px;">{{this.size}}</span>
                                </td>
                                <td class="cart__price" style="width: 160px;">
                                    {{formatCurrency this.price_at_purchase}} VND
                                </td>
                                <td class="cart__price" style="text-align:center;width:100px;" >
                                    {{this.quantity}}
                                </td>
                                <td class="cart__price" style="text-align:right;width:160px;">
                                    {{formatCurrency this.subtotal}} VND
                                </td>
                            </tr>
                        {{/each}}
                        </tbody>
                    </table>
                </div>

                <div class="row">
                    <div class="col-lg-12">
                        <div class="continue__btn">
                            <a href="/my-orders"><i class="fa fa-arrow-left"></i> Back to History</a>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Right Side: Order Summary & Info -->
            <div class="col-lg-4">
                <div class="cart__total" style="margin-top: 0; margin-bottom: 30px;">
                    <h6>Order Summary</h6>
                    <ul>
                        <li>Status
                            <span class="badge
                                {{#if (eq order.status 'PendingPayment')}}badge-warning text-dark{{/if}}
                                {{#if (eq order.status 'Paid')}}badge-info{{/if}}
                                {{#if (eq order.status 'Confirmed')}}badge-primary{{/if}}
                                {{#if (eq order.status 'Processing')}}badge-secondary{{/if}}
                                {{#if (eq order.status 'Shipping')}}badge-info{{/if}}
                                {{#if (eq order.status 'Delivered')}}badge-success{{/if}}
                                {{#if (eq order.status 'Cancelled')}}badge-danger{{/if}}"
                                  style="padding: 5px 10px; font-weight: 500;">
                                {{order.status}}
                            </span>
                        </li>
                        <li>Payment Status <span style="color: #111;">{{#if (eq order.status 'PendingPayment')}}Unpaid{{else}}Paid{{/if}}</span></li>
                        <li>Payment Method <span style="color: #111;">{{#if order.paymentMethod}}{{order.paymentMethod}}{{else}}Bank Transfer{{/if}}</span></li>
                        <li style="border-top: 1px solid #f2f2f2; padding-top: 15px; margin-top: 15px;">
                            Total
                            <span style="color: #d10024; font-size: 20px;">
                                {{formatCurrency order.totalPrice}} VND
                            </span>
                        </li>
                    </ul>

                    {{#if (eq order.status 'PendingPayment')}}
                        <form action="/cancel-order/{{order._id}}" method="POST" onsubmit="return confirm('Are you sure you want to cancel this order? This action cannot be undone.');">
                            <button type="submit" class="site-btn" style="background:#dc3545; width: 100%; margin-top: 20px; border-radius: 2px;">
                                Cancel Order
                            </button>
                        </form>
                    {{/if}}
                </div>

                <div class="cart__total" style="margin-top: 0;">
                    <h6>Delivery Information</h6>
                    <ul style="border-bottom: none;">
                        <li style="display: block; margin-bottom: 10px;">
                            <p style="margin-bottom: 0; font-weight: 600; color: #111;">Recipient</p>
                            <span style="float: none; font-size: 14px; color: #444;">{{order.shippingAddress.receiverName}}</span>
                        </li>
                        <li style="display: block; margin-bottom: 10px;">
                            <p style="margin-bottom: 0; font-weight: 600; color: #111;">Phone Number</p>
                            <span style="float: none; font-size: 14px; color: #444;">{{order.shippingAddress.receiverPhone}}</span>
                        </li>
                        <li style="display: block; margin-bottom: 10px;">
                            <p style="margin-bottom: 0; font-weight: 600; color: #111;">Address</p>
                            <span style="float: none; font-size: 14px; color: #444; line-height: 1.4;">
                                {{order.shippingAddress.detailAddress}}
                            </span>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</section>